import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);

//architecture
// client side rendered app;(cra);
//database from  an external REST API;
//react-loading-skeleton(external dependency)
//tailwind for styling
//installed firesbase but won't be using

//folder structure
//src
// components
//constants
//context
//helpers
//styles (tailwind's folder)
